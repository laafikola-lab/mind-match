export default function SwipeIndex() {
  return (
    <div style={{ padding: 40, maxWidth: 720, margin: "0 auto" }}>
      <h1>Discover — Test Page</h1>
      <p>If you see this, the /swipe route is deployed correctly.</p>
      <p>Now sign-in and test the real swipe UI at /swipe (should replace this soon).</p>
    </div>
  );
}
