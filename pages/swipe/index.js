export default function SwipeIndex() {
  return (
    <div style={{ padding: 40, maxWidth: 720, margin: "0 auto" }}>
      <h1>Discover — Test Page</h1>
      <p>If you see this, the <code>/swipe</code> route is deployed correctly.</p>
      <p>If this appears but the old UI is missing, we can move the real UI into this file next.</p>
    </div>
  );
}
